# Convert Slide Concept to Bridgeboard Template

> Turns a slide or framework into a dark glassboard template.

Convert the following slide / framework into a Bridgeboard dark-glass template.

Preserve the core logic, but redraw it as a calm, restrained board: positioned shapes, arrows and labels — never decorative. Use normalized `0..1` coordinates and the Bridge Work palette only.

**Framework:**
[INSERT FRAMEWORK]

Return only valid Bridgeboard template JSON following the schema.
